# Mapeador de Politicas Publicas Municipais
# Instale com:  pip install -r requirements.txt
#
# Python 3.10 ou superior. Testado em 3.13.5 no Windows 11.

# --- Interface grafica e busca (app/gui.py, app/busca.py) ---
# O Tkinter vem junto com o Python no Windows e no macOS.
# No Linux, instale o pacote do sistema:  sudo apt install python3-tk
pandas>=2.2.0            # exportacao dos resultados para Excel e CSV
openpyxl>=3.1.0          # escrita dos arquivos .xlsx
pillow>=10.0.0           # exibicao das fotos dos candidatos na ficha

# --- Construcao da base e leitura dos PDFs ---
# (app/construir_bd.py, app/auditar_qualidade.py)
PyMuPDF>=1.24.0          # extracao do texto dos planos de governo

# --- Reconhecimento optico dos planos escaneados (app/ocr_planos.py) ---
# Roda na CPU, sem instalacao de programa externo. Se voce nao pretende
# reprocessar planos escaneados, pode omitir estas duas linhas.
rapidocr-onnxruntime>=1.2.3
onnxruntime>=1.17.0

# --- Coleta no portal do TSE ---
# (coleta_dados.py, para 2024; app/coletar_2026.py, para 2026)
selenium>=4.20.0         # requer o Google Chrome instalado
requests>=2.31.0         # download do cadastro oficial em app/importar_tse.py

# --- Utilitarios do coletor de 2024 ---
PyPDF2>=3.0.0
unidecode>=1.3.8
screeninfo>=0.8.1
